<%- include('../partials/header') %>
    <ul>
        <% myDevSkills.forEach(function(s) { %>
            <li>
            <a href=<%= `/skills/${s.skill}` %>><%= s.skill %></a>
            </li>
        <% }); %>
    </ul>
        <hr>
        <a href="/skills/new">ADD a new skill to the list</a>
<%- include('../partials/footer') %>